{
  "name": "webpack-dev-middleware-koa",
  "version": "0.0.0",
  "description": "webpack-dev-middleware for koa",
  "main": "src/index.js",
  "scripts": {
    "release": "pnpm build && bumpp --push --tag --commit && npm publish"
  },
  "author": "crcong",
  "license": "MIC",
  "dependencies": {
    "webpack-dev-middleware": "^5.3.1"
  },
  "peerDependencies": {
    "webpack": "^4.0.0 || ^5.0.0"
  },
  "devDependencies": {
    "@crcong/eslint-config-basic": "^0.2.0",
    "eslint": "^8.11.0"
  }
}
